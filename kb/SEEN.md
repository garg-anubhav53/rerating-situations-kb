# SEEN — dedup guard

One `TICKER|YEAR` key per line for every name already captured. Scouts receive this list inline and
skip anything already here, so re-runs / resumed `PARTIAL` years never duplicate work.

<!-- keys below, one per line, e.g. NVDA|2023 -->
